#!/bin/sh

is_muted=$(amixer get Speaker | grep -E "Front Left:" | cut -d " " -f7 )
if [ $is_muted = "[off]" ];
then
	echo "MUTE"
else
	amixer get Speaker | grep -E "Front Left:" | cut -d " " -f7 | tr -d '[]'
fi
