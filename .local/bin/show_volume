#!/bin/sh

is_muted=$(amixer get Master | grep -E "Front Left:" | cut -d " " -f8 )
if [ $is_muted = "[off]" ];
then
	echo "MUTE"
else
	amixer get Master | grep -E "Front Left:" | cut -d " " -f6 | tr -d '[]'
fi
